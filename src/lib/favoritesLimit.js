// Single source of truth for the maximum number of favorites a client can save.
// Imported by both the Discover (like) and Favorites screens so the limit
// changes in one place.
export const MAX_FAVORITES = 50;